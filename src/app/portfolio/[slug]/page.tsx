import {db} from "@/lib/prisma";import {Header} from "@/components/header";import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await db.portfolio.findUnique({where:{slug}});if(!p)return notFound();return <><Header/><main className="mx-auto max-w-5xl px-5 pb-24 pt-40"><p className="text-xs uppercase tracking-[.3em] text-[var(--primary)]">{p.category}</p><h1 className="mt-4 font-display text-6xl font-bold">{p.title}</h1><p className="mt-8 text-xl leading-8 text-zinc-400">{p.excerpt}</p><div className="mt-12 aspect-[16/8] rounded-3xl border border-white/10 bg-gradient-to-br from-zinc-800 to-zinc-950"/><p className="mt-12 max-w-3xl text-lg leading-8 text-zinc-300">{p.description}</p></main></>}
