import {db} from "@/lib/prisma";import {Header} from "@/components/header";import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await db.blogPost.findUnique({where:{slug}});if(!p)return notFound();return <><Header/><article className="mx-auto max-w-3xl px-5 pb-24 pt-40"><p className="text-xs uppercase tracking-[.3em] text-[var(--primary)]">{p.category||"ARTICLE"}</p><h1 className="mt-4 font-display text-5xl font-bold md:text-7xl">{p.title}</h1><p className="mt-7 text-xl leading-8 text-zinc-400">{p.excerpt}</p><div className="mt-12 text-lg leading-8 text-zinc-300" dangerouslySetInnerHTML={{__html:p.content}}/></article></>}
