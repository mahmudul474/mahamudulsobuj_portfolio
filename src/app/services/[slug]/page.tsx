import {db} from "@/lib/prisma";import {Header} from "@/components/header";import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await db.service.findUnique({where:{slug}});if(!s)return notFound();return <><Header/><main className="mx-auto max-w-4xl px-5 pb-24 pt-40"><p className="text-xs uppercase tracking-[.3em] text-[var(--primary)]">Service</p><h1 className="mt-4 font-display text-6xl font-bold">{s.title}</h1><p className="mt-8 text-xl leading-8 text-zinc-400">{s.excerpt}</p><p className="mt-12 border-t border-white/10 pt-10 text-lg leading-8 text-zinc-300">{s.description}</p></main></>}
