'use client';import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");const r=useRouter();async function go(e:React.FormEvent){e.preventDefault();const x=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(x.ok)r.push("/admin");else setError("Invalid credentials")}return <main className="flex min-h-screen items-center justify-center bg-black px-5"><form onSubmit={go} className="w-full max-w-md rounded-3xl border border-white/10 p-8"><h1 className="font-display text-3xl font-bold">Admin Login</h1>{error&&<p className="mt-4 text-red-400">{error}</p>}<input className="mt-8 w-full rounded-xl border border-white/10 bg-transparent p-3" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="mt-3 w-full rounded-xl border border-white/10 bg-transparent p-3" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="mt-5 w-full rounded-xl bg-[var(--primary)] p-3 font-semibold text-black">Login</button></form></main>}
