import {db} from "@/lib/prisma";export default async function Page(){const x=await db.appointment.findMany({orderBy:{createdAt:"desc"}});return <div className="p-6 md:p-10"><h1 className="font-display text-4xl font-bold">Appointments</h1><div className="mt-8 divide-y divide-white/10 rounded-3xl border border-white/10">{x.map(i=><div key={i.id} className="p-5"><div className="flex justify-between"><b>{i.name}</b><span className="text-[var(--primary)]">{i.status}</span></div><p className="text-sm text-zinc-500">{i.email} · {i.date} {i.time}</p><p className="mt-2 text-sm text-zinc-400">{i.message}</p></div>)}</div></div>}
