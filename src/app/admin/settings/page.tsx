import {db} from "@/lib/prisma";export default async function Page(){const s=await db.siteSettings.findUnique({where:{id:"main"}});return <div className="p-6 md:p-10"><h1 className="font-display text-4xl font-bold">Theme Settings</h1><div className="mt-8 rounded-3xl border border-white/10 p-6 text-zinc-400">Site name: {s?.siteName}<br/>Primary: {s?.primary}<br/>Animations: {s?.animations?"On":"Off"}<br/>Cursor: {s?.cursor?"On":"Off"}<p className="mt-6 text-sm">Extend this panel with color pickers, logo upload, typography and navigation controls.</p></div></div>}