import {db} from "@/lib/prisma";import Link from "next/link";export default async function Page(){const x=await db.portfolio.findMany({orderBy:{createdAt:"desc"}});return <div className="p-6 md:p-10"><div className="flex justify-between"><h1 className="font-display text-4xl font-bold">Portfolio</h1><Link href="/admin/portfolio/new" className="rounded-full bg-[var(--primary)] px-5 py-2 text-black">+ Add</Link></div><div className="mt-8 divide-y divide-white/10 rounded-3xl border border-white/10">{x.map(i=><div key={i.id} className="p-5"><b>{i.title}</b><p className="text-sm text-zinc-500">{i.category} · {i.slug}</p></div>)}</div></div>}
