'use client';import {motion,useMotionValue,useSpring} from "framer-motion";import {useEffect} from "react";
export function Cursor(){const x=useMotionValue(-50),y=useMotionValue(-50),sx=useSpring(x,{stiffness:500,damping:35}),sy=useSpring(y,{stiffness:500,damping:35});useEffect(()=>{const f=(e:MouseEvent)=>{x.set(e.clientX-9);y.set(e.clientY-9)};addEventListener("mousemove",f);return()=>removeEventListener("mousemove",f)},[x,y]);return <motion.div className="pointer-events-none fixed z-[100] hidden h-5 w-5 rounded-full border border-white/60 mix-blend-difference md:block" style={{left:sx,top:sy}}/>}
