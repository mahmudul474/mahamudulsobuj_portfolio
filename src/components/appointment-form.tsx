'use client';import {useState} from "react";
export function AppointmentForm(){const [done,setDone]=useState(false);async function go(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/appointments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)});if(r.ok)setDone(true)}if(done)return <div className="mt-10 rounded-3xl border border-emerald-400/30 p-8">Request received. Thank you.</div>;return <form onSubmit={go} className="mt-10 space-y-4">{["name","email","service","date","time"].map(n=><input key={n} required={!["service"].includes(n)} name={n} type={n==="date"?"date":n==="time"?"time":n==="email"?"email":"text"} placeholder={n} className="w-full rounded-2xl border border-white/10 bg-white/[.03] px-5 py-4"/>) }<textarea name="message" rows={5} placeholder="Project details" className="w-full rounded-2xl border border-white/10 bg-white/[.03] px-5 py-4"/><button className="rounded-full bg-[var(--primary)] px-7 py-3 font-semibold text-black">Send request</button></form>}
